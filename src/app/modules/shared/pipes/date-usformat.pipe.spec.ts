import { DateUSFormatPipe } from './date-usformat.pipe';

describe('DateUSFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new DateUSFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
