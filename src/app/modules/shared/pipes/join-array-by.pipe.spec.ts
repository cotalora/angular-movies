import { JoinArrayByPipe } from './join-array-by.pipe';

describe('JoinArrayByPipe', () => {
  it('create an instance', () => {
    const pipe = new JoinArrayByPipe();
    expect(pipe).toBeTruthy();
  });
});
